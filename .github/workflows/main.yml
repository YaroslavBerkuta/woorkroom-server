name: 'Build and Deploy to Prod'

on:
  push:
    branches:
      - main

permissions: write-all

env:
  CONFLUENCE_USER: yaroslavberkuta@gmail.com
  CONFLUENCE_API_TOKEN: ATATT3xFfGF0JzzBXevdQWKiN4aZZw9aClbybNu3gHujsV3pyEymTDj_h2ihHugt67DegWQj5nMbdR3ZRzh0PHRV5-0zHdW7jwu8YAqB0zYHEQahjrX9mg-vJXnIZE7LesILUV0I-ScIueOfudvr_iv3FYe_Uy3rBSAmBmKe56T-Sxar8GnPSAY=803C79B7

jobs:
  task-reports:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      # Отримуємо всі коміти, які потрапили в поточну гілку
      - name: Get commit messages
        id: commits
        run: |
          git fetch --prune
          branch_name="${GITHUB_REF#refs/heads/}"
          commits=$(git log --pretty=format:"%s" origin/$branch_name..HEAD)
          echo "commits=$commits" >> $GITHUB_ENV

      # Витягування ключів задач з повідомлень комітів
      - name: Extract task keys from commit messages
        id: extract_keys
        run: |
          keys=$(echo "${{ env.commits }}" | grep -oE '\[([A-Za-z]+-[0-9]+)\]' | sort | uniq)
          echo "task_keys=$keys" >> $GITHUB_ENV

      # Формуємо звіт для Confluence
      - name: Create Confluence report
        run: |
          report="### Deploy Report\n\n**Version**: ${{ github.sha }}\n\n**Tasks in this build**: ${{ env.task_keys }}"

          curl --location 'https://fanscrm.atlassian.net/wiki/api/v2/pages' \
            --header 'Content-Type: application/json' \
            --header 'Accept: application/json' \
            --header 'Authorization: Basic $(echo -n "${{ env.CONFLUENCE_USER }}:${{ env.CONFLUENCE_API_TOKEN }}" | base64)' \
            --data '{
              "spaceId": "1802243",
              "status": "current",
              "title": "Build Report for Version '${{ github.sha }}'",
              "parentId": "420642853",
              "body": {
                "representation": "storage",
                "value": "'"$report"'"
              }
            }'
